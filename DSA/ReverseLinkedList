class ListNode{
  int val;
  ListNode next;
  ListNode(int val){  
    this.val = val; 
    }
  }

 class solution{
     public ListNode reverseList(ListNode head){
         ListNode prev= null, curr= head;
         while(curr!=null){
             ListNode next = curr.next;
              current.next= prev;
              prev=curr;
              curr=next;
           }
       return prev;
       }
   }
